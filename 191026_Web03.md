1. C
2. Math与JSON
3. var date1 = new Date();
   var month = date1.getMonth() + 1;
   console.log(month);
4. A
5. D 要求首字母大写
6. 
````js

````
7. 
````js

````
###
````js
var chapterList = {

    list: [

        {id: 2, chapterName: "文章2"},

        {id: 1, chapterName: "文章1"},

        {id: 4, chapterName: "文章4"},

        {id: 3, chapterName: "文章3"}

    ]

}
var temp = {};
var change =true;
for(var i = chapterList.list.length - 1;i >= 1 && change;i--){
    change = false;
    for(var j = 0;j < i;j++){
        if(chapterList.list[j].id > chapterList.list[j+1].id){
            temp=chapterList.list[j];
            chapterList.list[j] = chapterList.list[j+1];
            chapterList.list[j+1] = temp;
            change = true;
        }
    }
}
for(var k = 0; k < chapterList.list.length;k++){
    console.log(chapterList.list[k]);
}
````
### 
````js
var timer;
function getTime(format){
    var date1 = new Date();
    var tempdate1 = date1.toLocaleDateString().substr(0,10);
    var tempFormat = format.split("");
    var temp = [];
    var Y = 0;
    var M = 0;
    var D = 0;
    var j = 0;
    for(var i = 0; i < tempFormat.length;i++){
        if(tempFormat[i] == 'Y'){
            Y++;
            break;
        }
        else if(tempFormat[i] == 'M'){
            M++;
            break;
        }
        else if(tempFormat[i] == 'D'){
            D++;
            break;
        }
        else{
            temp[j] = tempFormat[i];
            j++;
        }
        // var printdate1 = {};
    }
    console.log(temp);   
}
getTime("YYYY-MM-DD");
// var date1 = new Date();
// console.log(date1.toLocaleString());
// var temp = date1.toLocaleDateString().substr(0,10);
// var arr =temp.split("");
// console.log(arr.length);  
````
